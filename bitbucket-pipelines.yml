pipelines:
  default:
    - step:
        name: Alert Opsgenie
        script:
        - pipe: atlassian/google-app-engine-deploy:0.7.4
          variables:
            KEY_FILE: $KEY_FILE
            PROJECT: $PROJECT